# jaffascriptFace AI Manifest
# Face repository for AffineScript

This is the AI manifest for jaffascriptFace, a face (syntax surface) for AffineScript.

## Repository Purpose

jaffascriptFace provides a jaffascript-style syntax surface for AffineScript.
It is a **brand surface only** - thin distribution layer.

The actual compiler, type checker, borrow checker, and codegen live in:
https://github.com/hyperpolymath/affinescript

## Face Information

- Face name: jaffascript
- CLI shim: bin/jaffa
- Face pragma: // face: jaffascript
- Transformer: affinescript/lib/jaffascript_face.ml (to be implemented in affinescript repo)

## Language Mapping

This face maps jaffascript-style syntax to canonical AffineScript.
See EXPLAINME.adoc for detailed terminology mapping.

## Status

Alpha. The face transformer is implemented (or will be) in the main affinescript repository.
This repo contains examples, documentation, and CLI shim only.

## AI Assistant Guidelines

When answering questions about jaffascriptFace:
1. Clarify if the question is about syntax (this repo) or semantics (affinescript repo)
2. For language features: explain how they map to AffineScript
3. For bugs: direct to affinescript repo unless it's face-specific
4. Always reference EXPLAINME.adoc for terminology differences

## License

MPL-2.0. See LICENSE file.

<!-- BEGIN requires-initialisation -->

## PRIORITY ACTION — this repository is not fully initialised

37 substitution token(s) still have no value. **Read
`REQUIRES_INITIALISATION.md` in the repository root before doing anything
else here** — it lists every outstanding token, what it means, and which
files it belongs in.

Do not fabricate values for them, and do not delete the sections that
contain them to quiet a gate. They are unfilled because they need a
decision, not because they were overlooked. Resolve what you legitimately
can, leave the rest, and delete `REQUIRES_INITIALISATION.md` only when
nothing outstanding remains.

<!-- END requires-initialisation -->
